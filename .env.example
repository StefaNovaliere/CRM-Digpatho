# ============================================
# Digpatho CRM - Environment Variables
# ============================================
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to version control.
# ============================================

# Supabase (REQUIRED)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Anthropic API (OPTIONAL if using Edge Function proxy)
# If empty, the app will use the supabase/functions/anthropic-proxy Edge Function.
# This is the RECOMMENDED setup for production - keeps the key server-side.
VITE_ANTHROPIC_API_KEY=

# Google OAuth (OPTIONAL - only needed for Gmail integration)
VITE_GOOGLE_CLIENT_ID=your-google-client-id
VITE_GOOGLE_CLIENT_SECRET=your-google-client-secret

# Resend Email Service (OPTIONAL)
VITE_RESEND_API_KEY=
VITE_EMAIL_FROM=Digpatho CRM <noreply@digpatho.com>

# App Config
VITE_APP_NAME=Digpatho CRM
