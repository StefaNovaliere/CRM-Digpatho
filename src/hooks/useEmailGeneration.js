/**
 * BACKWARD COMPATIBILITY REDIRECT
 * New code should import from: '../presentation/hooks/useEmailGeneration'
 */
export { useEmailGeneration } from '../presentation/hooks/useEmailGeneration';
export { useEmailGeneration as default } from '../presentation/hooks/useEmailGeneration';
